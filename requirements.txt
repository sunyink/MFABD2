json-with-comments
requests>=2.25.0
pytz
loguru
numpy<2
Pillow>=9.0.0

# -------------------------------------------------------
# [核心版本配置]
# 1. 指定资源包版本 (CI 读取此行下载资源)
# MFAA_TAG=v2.15.2
#
# 1.5 [急救覆盖] MFA 内核版本覆盖 (值为 MaaXYZ/MaaFramework 的 release tag，带 v)
#     留空 = 不覆盖 (等同注释掉，沿用 MFAA 自带内核)
#     填写 = 急救模式，MFAA 装完后再拉此版本内核【同路径覆盖】，并同步 pip/安卓/资源检查
#     例: # MFA_CORE_TAG=v5.12.2
#     当前填值的原因：MFAAvalonia v2.15.2 自带 5.12.2，而安卓上游 MaaFwApp 钉的
#     agent core 是 3.13.15-maafw5.12.3。安卓与桌面必须同版本，且 agent core 至今
#     只发过这一个，所以 5.12.3 是唯一的交汇点。换 MaaFwApp 的 pin 时要一起重估这行。
# MFA_CORE_TAG=v5.12.3
#
# 2. 这里的 pip 版本暂时由人工维护，需匹配上方 Tag 对应的 Core 版本
#    (Meta 任务会自动探测校验，如果不匹配 CI 会报错/警告)
# -------------------------------------------------------